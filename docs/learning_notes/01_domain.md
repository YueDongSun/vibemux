# Domain lab

Question: why separate Task and Run? Concept: a Task is the collaboration intent; a Run is one stoppable execution. Experiment: create a Task and spawn two mock Runs, then compare the different branches/panes. Teach-back: why can't a Run write directly to main? Why is terminal exit not equivalent to success? Why are Events independent of WezTerm JSON? Next step: read `storage` and `workspace`.
