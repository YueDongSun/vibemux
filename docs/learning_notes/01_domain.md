# Domain lab

问题：为什么 Task 和 Run 分离？概念：Task 是协作意图，Run 是一次可停止执行。实验：创建一个 task 并 spawn 两个 mock run，比较不同 branch/pane。Teach-back：Run 为什么不能直接写入 main？terminal exit 为什么不等于 success？Event 为什么独立于 WezTerm JSON？下一步：阅读 storage 与 workspace。

