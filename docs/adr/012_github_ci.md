# ADR 012: GitHub publication and Windows/Linux CI

Context: Windows is formal target while Linux is development compatibility. Decision: Actions matrix tests both on every push. Consequences: platform regressions are visible. Alternative: Linux-only CI, rejected.

