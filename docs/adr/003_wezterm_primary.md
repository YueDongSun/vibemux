# ADR 003: WezTerm primary Windows backend

Context: Windows needs programmable panes. Decision: WezTerm CLI is first backend. Consequences: live integration depends on external binary. Alternative: wt.exe UI automation, rejected.

