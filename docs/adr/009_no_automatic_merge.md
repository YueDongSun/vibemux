# ADR 009: No automatic merge

Context: agent changes require human review. Decision: VibeMux exposes diff and never auto commits, merges or pushes. Consequences: safer workflow. Alternative: background merge queue, rejected.

