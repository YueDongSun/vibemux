# ADR 006: Canonical event model

Context: terminal/protocol outputs vary. Decision: persist platform-neutral Event records. Consequences: replay and trace are stable. Alternative: store raw terminal JSON as domain events, rejected.

